{
  "name": "coursebuddy",
  "private": true,
  "description": "Deploy entry point (Railway/Nixpacks): installs both apps, builds the React frontend, then runs the Express backend which serves it.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run install:all && npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  }
}
